/**
 * This file is the directory of the component
 * The purpose is to uniformly manage the components of external output, which is convenient for classification
 */
/**
 * Layout component
 */
import Footer from './Footer';
import TextEditor from './TextEditor';
import { Question, SelectLang } from './RightContent';
import { AvatarDropdown, AvatarName } from './RightContent/AvatarDropdown';

export { Footer, Question, SelectLang, AvatarDropdown, AvatarName, TextEditor };
